---
phase: 04-search-retrieval
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/alembic/versions/0003_add_fts_and_trgm.py
  - backend/app/models/document.py
  - backend/tests/test_search.py
autonomous: true
requirements:
  - SRCH-01
  - SRCH-04

must_haves:
  truths:
    - "The documents table has a search_vector TSVECTOR column after migration"
    - "A GIN index exists on search_vector for fast FTS lookups"
    - "A GIN trigram index exists on extracted_text for fuzzy matching"
    - "A trigger keeps search_vector in sync on INSERT and UPDATE"
    - "All existing completed documents have search_vector backfilled (not NULL)"
    - "pg_trgm extension is enabled in the database"
    - "Test scaffold exists with stubs for all SRCH-01 through SRCH-04 test cases"
  artifacts:
    - path: "backend/alembic/versions/0003_add_fts_and_trgm.py"
      provides: "Alembic migration adding tsvector column, GIN indexes, trigger, pg_trgm"
      contains: "CREATE EXTENSION IF NOT EXISTS pg_trgm"
    - path: "backend/app/models/document.py"
      provides: "Document model with search_vector TSVECTOR column"
      contains: "search_vector"
    - path: "backend/tests/test_search.py"
      provides: "Test stubs for SRCH-01 through SRCH-04"
      exports: ["test_fts_returns_ranked_results", "test_fts_relevance_ranking", "test_search_with_category_filter", "test_search_with_date_filter", "test_search_with_amount_filter", "test_fuzzy_typo_matching", "test_search_performance"]
  key_links:
    - from: "trigger documents_search_vector_trigger"
      to: "documents.search_vector"
      via: "BEFORE INSERT OR UPDATE"
      pattern: "documents_search_vector_trigger"
    - from: "Document model"
      to: "alembic autogenerate"
      via: "search_vector column declared WITHOUT Index in __table_args__"
      pattern: "search_vector.*TSVECTOR"
---

<objective>
Lay the database foundation for full-text search: add a stored TSVECTOR column to documents, create GIN indexes for FTS and fuzzy matching, and create the test scaffold that all subsequent plans verify against.

Purpose: Without the schema changes and indexes, the search endpoint has no performant path to FTS or fuzzy matching. Without the test scaffold (Nyquist Wave 0), plans 02 and 03 cannot have automated verify commands.

Output: Alembic migration 0003, updated Document model, test scaffold with stubs.
</objective>

<execution_context>
@C:/Users/polli/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/polli/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-search-retrieval/04-RESEARCH.md
@.planning/phases/04-search-retrieval/04-VALIDATION.md

<interfaces>
<!-- Existing Document model columns the executor needs to know about -->
<!-- From backend/app/models/document.py -->
```python
class DocumentCategory(str, enum.Enum):  # utility_bill, upi_transaction, travel_ticket, tax_document, bank_statement, invoice

class DocumentStatus(str, enum.Enum):    # pending, processing, completed, failed

class Document(Base):
    __tablename__ = "documents"
    id = Column(Integer, primary_key=True)
    user_id = Column(Integer, ForeignKey("users.id"))
    original_filename = Column(String(500), nullable=False)
    extracted_text = Column(Text, nullable=True)
    extracted_metadata = Column(JSON, nullable=True)   # JSONB; may contain "amount" key
    category = Column(Enum(DocumentCategory), nullable=True)
    status = Column(Enum(DocumentStatus), nullable=False)
    created_at = Column(DateTime, nullable=False)
    # search_vector — does NOT exist yet; this plan adds it
```

<!-- Existing migrations for context -->
<!-- backend/alembic/versions/ contains: -->
<!-- 097ce00eb065_initial_schema_users_documents_refresh_.py -->
<!-- 0002_add_metadata_and_celery_fields.py -->
<!-- New migration will be: 0003_add_fts_and_trgm.py -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write test scaffold for SRCH-01 through SRCH-04</name>
  <files>backend/tests/test_search.py</files>
  <action>
Create `backend/tests/test_search.py` with stub test functions. All stubs must fail with a clear pytest.skip or xfail marker (NOT raise NotImplementedError — that crashes the runner before Wave 0 is confirmed green). Use `pytest.mark.skip(reason="stub — implement in plan 04-02")` on all stubs.

Required test functions (exact names, used in VALIDATION.md verify commands):
- `test_fts_returns_ranked_results` — stub (SRCH-01): will verify FTS match returns documents containing query term
- `test_fts_relevance_ranking` — stub (SRCH-01): will verify ts_rank orders most-relevant first
- `test_search_with_category_filter` — stub (SRCH-02): will verify category filter narrows results
- `test_search_with_date_filter` — stub (SRCH-02): will verify date_from/date_to params filter by created_at
- `test_search_with_amount_filter` — stub (SRCH-02): will verify amount_min/amount_max filters on extracted_metadata["amount"]
- `test_fuzzy_typo_matching` — stub (SRCH-03): will verify "electrcity" query returns electricity documents
- `test_search_performance` — stub (SRCH-04): will verify search returns in under 2 seconds

Add a module docstring explaining: "FTS/trigram tests (test_fuzzy_typo_matching, test_search_performance) require a real PostgreSQL instance — they skip on SQLite conftest."

No imports of production code are needed for stubs. Keep the file minimal.
  </action>
  <verify>
    <automated>cd "C:/Users/polli/OneDrive/Desktop/SMART DOCUMENT MANAGEMENT SYSTEM- IIITHYD PROD LABS/backend" && python -m pytest tests/test_search.py -v --co 2>&1 | head -30</automated>
  </verify>
  <done>pytest --collect-only shows all 7 test function names collected; running the file shows all 7 as SKIPPED (not ERROR, not FAILED)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Alembic migration — tsvector column, GIN indexes, trigger, pg_trgm</name>
  <files>backend/alembic/versions/0003_add_fts_and_trgm.py</files>
  <behavior>
    - After `alembic upgrade head` runs: `search_vector` column exists on documents table with TSVECTOR type
    - After migration: `idx_documents_search_vector` GIN index exists on search_vector
    - After migration: `idx_documents_trgm` GIN index exists on extracted_text with gin_trgm_ops operator class
    - After migration: pg_trgm extension is enabled (pg_catalog.pg_extension contains 'pg_trgm')
    - After migration: `documents_search_vector_trigger` BEFORE INSERT OR UPDATE trigger exists on documents table
    - After migration: all rows where status='completed' have non-NULL search_vector
    - After `alembic downgrade -1`: all of the above are removed cleanly
  </behavior>
  <action>
Create `backend/alembic/versions/0003_add_fts_and_trgm.py` using EXACTLY the pattern from 04-RESEARCH.md "Pattern 1: Stored TSVECTOR Column with Trigger".

Key implementation rules:
1. Use `op.execute()` for ALL DDL (extension, GIN indexes, trigger function, trigger attachment) — do NOT use SQLAlchemy `Index(...)` objects in this file. This avoids the Alembic autogenerate false-diff bug (issue #1390).
2. Import `from sqlalchemy.dialects.postgresql import TSVECTOR` for the column type.
3. Backfill query: `UPDATE documents SET search_vector = to_tsvector('english', COALESCE(extracted_text, '') || ' ' || COALESCE(original_filename, '')) WHERE status = 'completed'`
4. Trigger concatenates `COALESCE(NEW.extracted_text, '')` and `COALESCE(NEW.original_filename, '')` separated by a space.
5. In `downgrade()`: drop trigger, drop trigger function, drop idx_documents_trgm, drop idx_documents_search_vector, drop_column search_vector, drop extension pg_trgm — in that order.
6. Set `revision = "0003_add_fts_and_trgm"` and `down_revision = "0002_add_metadata_and_celery_fields"` (chain from last migration).

Do NOT define the GIN index on search_vector in Document.__table_args__ — that belongs only in this migration file.
  </action>
  <verify>
    <automated>cd "C:/Users/polli/OneDrive/Desktop/SMART DOCUMENT MANAGEMENT SYSTEM- IIITHYD PROD LABS/backend" && python -c "import alembic.config; from alembic.script import ScriptDirectory; cfg = alembic.config.Config('alembic.ini'); sd = ScriptDirectory.from_config(cfg); revs = list(sd.walk_revisions()); print([r.revision for r in revs])"</automated>
  </verify>
  <done>Migration file imports without error; revision chain includes 0003_add_fts_and_trgm; alembic upgrade head applies cleanly against the running database</done>
</task>

<task type="auto">
  <name>Task 3: Add search_vector column to Document model</name>
  <files>backend/app/models/document.py</files>
  <action>
Add `search_vector` column to the `Document` class in `backend/app/models/document.py`:

```python
from sqlalchemy.dialects.postgresql import TSVECTOR

class Document(Base):
    # ... existing columns unchanged ...
    search_vector = Column(TSVECTOR, nullable=True)
```

CRITICAL: Do NOT add any `Index(...)` for `search_vector` anywhere in this file or in `__table_args__`. The GIN index is managed exclusively by the migration. Adding a SQLAlchemy Index here triggers the Alembic false-diff bug (issue #1390).

The column is nullable=True because: (a) documents in non-completed states have no extracted_text yet, (b) the trigger will populate it on the next INSERT or UPDATE, and (c) the backfill in the migration handles existing rows.

After adding the column, verify the model file still parses cleanly with a Python import check.
  </action>
  <verify>
    <automated>cd "C:/Users/polli/OneDrive/Desktop/SMART DOCUMENT MANAGEMENT SYSTEM- IIITHYD PROD LABS/backend" && python -c "from app.models.document import Document; print('search_vector' in [c.name for c in Document.__table__.columns])"</automated>
  </verify>
  <done>Python import succeeds; output is "True"; no Index on search_vector in __table_args__</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `cd backend && python -m pytest tests/test_search.py -v` — all 7 tests collected and SKIPPED (not ERROR)
2. `cd backend && python -c "from app.models.document import Document; print('search_vector' in [c.name for c in Document.__table__.columns])"` — prints True
3. `cd backend && python -m alembic upgrade head` — migration applies without error
4. `cd backend && python -m pytest tests/ -v` — full suite passes (no regressions)
</verification>

<success_criteria>
- search_vector TSVECTOR column exists on Document model and in database after migration
- GIN index on search_vector (FTS) and GIN trigram index on extracted_text both created by migration
- pg_trgm extension enabled; trigger keeps search_vector current on INSERT/UPDATE
- All existing completed documents have non-NULL search_vector after backfill
- Test scaffold has all 7 test stubs collected and skipped (not erroring)
- Running `alembic revision --autogenerate` after this plan produces NO changes (no false diffs from tsvector index)
</success_criteria>

<output>
After completion, create `.planning/phases/04-search-retrieval/04-01-SUMMARY.md`
</output>
