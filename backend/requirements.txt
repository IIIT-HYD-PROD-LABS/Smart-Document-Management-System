# FastAPI & Server
# Phase 15 reconciliation: fastapi 0.120.4 + starlette 0.49.3 + anyio 4.13.0 are
# the joint minimum that satisfies fastmcp 3.2.4's transitive deps (mcp 1.27.0
# wants anyio>=4.5; sse-starlette 3.4.2 wants starlette>=0.49.1).
# pip check is clean at these pins.
fastapi==0.120.4
starlette==0.49.3
anyio==4.13.0
uvicorn[standard]==0.46.0  # Phase 15: bumped from 0.24.0 to satisfy fastmcp 3.2.4 (uvicorn>=0.35)
python-multipart==0.0.26

# Database
sqlalchemy==2.0.23
psycopg2-binary==2.9.9
alembic==1.18.4

# Authentication
PyJWT==2.12.1
bcrypt==4.1.2
passlib[bcrypt]==1.7.4

# ML & NLP
scikit-learn==1.6.1
numpy==1.26.2
pandas==2.1.4
joblib==1.3.2

# Phase 10 — Compliance ML (BERT classifier, NER, XGBoost risk scoring + SHAP)
# Torch CPU-only wheel to keep image under 3GB (CUDA libs not needed at our scale).
# Loaded only by compliance-worker (separate Celery service) — v1.0 worker stays lean.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.8.0
transformers==5.7.0
spacy==3.7.5
xgboost==2.1.3
shap==0.46.0
dateparser==1.2.0
Babel==2.16.0

# Phase 10 — SEBI / RBI training data scrapers (datasets/scrape_*.py)
beautifulsoup4==4.12.3
lxml==6.1.0

# Phase 11 — alert pipeline (APScheduler durable scheduler, Twilio SMS adapter,
# Indian holiday calendar). SendGrid migration deferred to v2.1 — keeps v2.0
# email path on the existing Resend SMTP integration.
APScheduler==3.11.0
twilio==9.4.4
holidays==0.61

# Phase 15 — Gmail MCP Integration (FastMCP server, Gmail API client,
# google-auth-oauthlib for web OAuth flow). google-auth pulled transitively.
fastmcp==3.2.4
google-api-python-client==2.196.0
google-auth-oauthlib==1.4.0

# OCR & Image Processing
pytesseract==0.3.10
opencv-python-headless==4.8.1.78
Pillow==12.2.0

# PDF Processing
pdfplumber==0.10.3

# DOCX Processing
python-docx==1.1.2

# AWS S3
boto3==1.33.6

# Celery & Redis
celery==5.3.6
redis==5.0.1

# Configuration
python-dotenv==1.2.2
pydantic==2.13.4  # Phase 15: bumped from 2.11.3 to match live container resolution under fastmcp 3.2.4
pydantic-settings==2.13.0

# Security & Rate Limiting
slowapi==0.1.9

# Logging
structlog==25.5.0
asgi-correlation-id==4.3.4

# HTTP Client
httpx==0.28.1  # Phase 15: bumped from 0.25.2 to satisfy fastmcp 3.2.4 (httpx>=0.28.1,<1.0)
pytest-freezer==0.4.9
freezegun==1.5.5

# Dataset Management
kaggle==1.6.17

# LLM Providers (optional - install if using LLM extraction)
anthropic==0.101.0
openai==1.86.0

# Utilities
python-dateutil==2.8.2

# ── Testing (not needed in production) ──────────────────────────
pytest==9.0.3
# pytest-asyncio < 1.3 has two issues with pytest 9:
#   - 0.21 - 0.23.x crash collection ("'Package' object has no attribute 'obj'")
#   - 0.24 - 1.2.x are pinned to pytest <9 in their setup metadata, so a fresh
#     `pip install` (strict resolver, e.g. Docker build) refuses to install them
#     alongside pytest==9.0.3 (ResolutionImpossible).
# 1.3.0 is the first release that declares `pytest<10,>=8.2` and works.
pytest-asyncio>=1.3,<2.0
