__pycache__
*.pyc
venv/
uploads/
# ML trained model weights (not app/models/ which contains SQLAlchemy models)
/models/
*.egg-info
# Manual-test sample documents (may contain real/PII notices — never commit)
tests/fixtures/
