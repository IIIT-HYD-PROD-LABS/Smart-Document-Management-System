import BillDashboard from "@/components/email/BillDashboard";

/**
 * /dashboard/email/bills — BILL-03 dashboard page.
 *
 * Thin server component shell; all interactivity lives in BillDashboard
 * (client component).
 */
export default function BillsPage() {
    return <BillDashboard />;
}
