// Phase 15 — EMAIL-04 filter rules CRUD tests.
// RED-state stub. Plan 06 lands vitest config + FilterRulesEditor component.
import { describe, it } from "vitest";

describe.skip("FilterRulesEditor (Plan 06)", () => {
  it.todo("lists existing gmail_filter_rules ordered by priority ascending");
  it.todo("creates a new rule via the add form");
  it.todo("updates priority via drag-handle ordering");
  it.todo("deletes a rule with confirmation modal");
});
